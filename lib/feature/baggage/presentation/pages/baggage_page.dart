import 'package:flutter/material.dart';

class BaggagePage extends StatelessWidget {
  const BaggagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Baggage"),),
    );
  }
}